# ADR-006: Упрощенная авторизация без пароля

**Статус:** Принято (временно для MVP)
**Дата:** 2024-01
**Автор:** Команда разработки

## Контекст

Для MVP корпоративного приложения карпулинга необходимо обеспечить быстрый онбординг пользователей. Учитывая:
- Закрытый контур использования (только сотрудники NLMK)
- Ограниченные сроки разработки MVP
- Необходимость минимизировать барьеры для входа
- Отсутствие критичных данных на начальном этапе

## Решение

Реализована **упрощенная аутентификация по email без пароля** с автоматической регистрацией.

```
┌──────────────────────────────────────────────────────────────┐
│                    Поток авто-регистрации                     │
└──────────────────────────────────────────────────────────────┘

Пользователь вводит email
         │
         ▼
    ┌─────────────┐
    │ POST /login │
    │ { email }   │
    └─────────────┘
         │
         ▼
    ┌─────────────────────────┐
    │ Пользователь существует? │
    └─────────────────────────┘
         │
    ┌────┴────┐
    │         │
   Да        Нет
    │         │
    ▼         ▼
 Вернуть   Создать нового
 токен     пользователя
              │
              ▼
           Вернуть токен
```

### Реализация

```typescript
// backend/src/routes/auth.ts
router.post('/login', async (req, res) => {
  const { email } = req.body;

  // Найти или создать пользователя
  let user = await prisma.user.findUnique({ where: { email } });

  if (!user) {
    user = await prisma.user.create({
      data: {
        email,
        name: email.split('@')[0],
        avatarUrl: `https://api.dicebear.com/7.x/avataaars/svg?seed=${email}`,
        role: 'Both',
        homeCity: 'Moscow'
      }
    });
  }

  const token = jwt.sign({ userId: user.id, email }, JWT_SECRET, { expiresIn: '7d' });
  res.json({ token, user });
});
```

## Альтернативы

### 1. Email + Password
- **Плюсы**: Стандартный подход, понятен пользователям
- **Минусы**: Требует восстановление пароля, хранение хешей, complexity requirements

### 2. Magic Link (email verification)
- **Плюсы**: Безопасно, без паролей, верификация email
- **Минусы**: Требует email-сервис (SendGrid, etc.), задержка входа

### 3. Корпоративный SSO (LDAP/AD)
- **Плюсы**: Единая точка входа, безопасность enterprise-уровня
- **Минусы**: Сложная интеграция, зависимость от IT-инфраструктуры

### 4. OAuth через корпоративный провайдер
- **Плюсы**: Безопасно, делегированная аутентификация
- **Минусы**: Требует настройки OAuth провайдера

## Последствия

### Положительные
- **Мгновенный онбординг**: Пользователь вводит email и сразу в системе
- **Нулевой барьер**: Не нужно придумывать/запоминать пароль
- **Быстрая разработка**: Минимум кода для MVP
- **Нет восстановления пароля**: Упрощает UX и поддержку

### Отрицательные
- **Нет верификации**: Любой может зайти под чужим email
- **Нет защиты аккаунта**: Невозможно "выйти" с других устройств
- **Не production-ready**: Недопустимо для публичного релиза
- **Репутационные риски**: При утечке данных

### Риски безопасности

| Риск | Уровень | Описание |
|------|---------|----------|
| Impersonation | 🔴 Высокий | Можно войти под любым email |
| Account takeover | 🔴 Высокий | Нет механизма защиты аккаунта |
| Data exposure | 🟡 Средний | Чужие поездки/отзывы видны |
| Spam accounts | 🟡 Средний | Нет ограничений на регистрацию |

### План миграции на безопасную аутентификацию

```
MVP (текущее состояние)
    │
    ▼
Этап 1: Magic Link
    - Интеграция email-сервиса
    - Отправка ссылки для входа
    - Верификация email
    │
    ▼
Этап 2: Корпоративный SSO
    - Интеграция с NLMK Active Directory
    - OAuth 2.0 / SAML
    - Автоматический provisioning
```

### Технический долг

- [ ] **КРИТИЧНО**: Реализовать Magic Link до публичного релиза
- [ ] Добавить email verification
- [ ] Интегрировать корпоративный SSO
- [ ] Добавить rate limiting на /login
- [ ] Логировать попытки входа

## Связанные решения

- ADR-005: JWT-аутентификация
- ADR-008: CORS-политика
